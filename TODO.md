# TODO

## 우선순위 높음 — 디자인보다 먼저

디자인 감사(design audit) 버킷 A에서 코드로 못 고치고 남은 3건. 셋 다 사용자 입력이 있어야 진행된다.

### 1. 제휴 링크 공시 (A11) — 법적 이슈
- 쿠팡 아웃바운드 링크 14개에 `rel="sponsored"`는 붙어 있으나, **공시 문구가 어디에도 없다**
- 공정위 기준상 대가성 표기 필요 (FTC도 동일)
- 함께 없는 것: 개인정보처리방침, 이용약관
- → 푸터에 공시 + 정책 페이지 2개 추가

### 2. 영어 이름·리뷰 채우기 (A3)
- `public/data.json`의 `name_en` / `review_en`이 **전 항목 비어 있음** (17개 중 17개)
- 그래서 영어 페이지에 한국어 제품명이 그대로 노출됨 — "English-friendly guide"라는 사이트 전제가 깨진다
- 정렬 수정(A20)의 효과도 이것 때문에 반감됨
- → Notion DB에서 16개 값 입력. 파이프라인(`fetch_notion.py` → data.json)은 이미 컬럼을 읽고 있으므로 값만 채우면 된다
- 참고: 이 항목의 과거 메모에 있던 `index.html` / `enNameMap` / `enReviewMap` 관련 작업은 Astro 이전으로 이미 무효

### 3. 사진 (A25)
- 사이트에 실제 이미지가 **0장**, 8개 페이지에 점선 플레이스홀더 10개
- 음식 사이트인데 음식 사진이 없다. 어떤 디자인을 골라도 이게 상한선을 만든다
- → 촬영 필요

---

## 디자인

### 4. 리디자인 방향 확정
- `redesign-taste` 브랜치에 3안 비교(bake-off) 완료: `/redesign` 에서 A/B/C 비교
  - A `minimalist-ui` — 웜 모노크롬, 에디토리얼 세리프
  - B `high-end-visual-design` — 크림/에스프레소, 더블 베젤, 시네마틱 모션
  - C `industrial-brutalist-ui` — 스위스 인더스트리얼 프린트, 대문자 Archivo, 해저드 레드
- **현재 선호: C.** 다만 그대로 쓰기엔 부족하고 다음 세션에서 디자인을 더 다듬어야 함
- 세 안 공통으로 남은 것:
  - 모바일 내비게이션 없음 (46~52rem 이하에서 메뉴 링크가 사라지고 햄버거가 없음)
  - 폰트가 외부 CDN(Google Fonts / Fontshare) — 채택 시 `@fontsource`로 옮길 것
- 버킷 B(AI 지문 14건)는 리디자인이 대체하므로 폐기

---

## 나중에

### 5. 상세 페이지
- 행 클릭 시 개별 상세 페이지/모달 표시
- Notion DB의 Description 등 상세 내용 포함

### 6. 커스텀 도메인
- 커스텀 도메인 연결
- 우선순위 낮음
